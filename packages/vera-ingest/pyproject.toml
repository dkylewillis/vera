[project]
name = "vera-ingest"
version = "0.3.1"
description = "Source ingestion, chunking, and conversion adapters for VERA"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "vera-doc>=0.3.1",
]
authors = [{name = "Kyle Willis"}]
license = {text = "Apache-2.0"}
keywords = ["pdf", "markdown", "chunking", "semantic-search", "vera"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Topic :: Text Processing :: Indexing",
]

[project.entry-points."vera.ingest_pipelines"]
markdown = "vera_ingest.markdown:create_pipeline"

[project.entry-points."vera.ingest_pipeline_descriptors"]
markdown = "vera_ingest.markdown:create_descriptor"

[project.urls]
Homepage = "https://github.com/dkylewillis/vera"
Repository = "https://github.com/dkylewillis/vera"
Documentation = "https://dkylewillis.github.io/vera/packages/vera-ingest/"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vera_ingest"]

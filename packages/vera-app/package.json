{
  "name": "@vera/app",
  "version": "0.1.0",
  "description": "Desktop workspace for VERA documents",
  "author": "Kyle Willis",
  "private": true,
  "type": "module",
  "main": "dist-electron/electron/main.js",
  "scripts": {
    "predev": "npm run build:electron && npm run typecheck:renderer",
    "dev": "concurrently -k --success first \"vite --host 127.0.0.1 --strictPort\" \"wait-on http://127.0.0.1:5173 && electron .\"",
    "build": "npm run build:electron && npm run typecheck:renderer && vite build",
    "build:electron": "tsc -p tsconfig.electron.json",
    "build:sidecar": "uv run --project ../.. --extra app --with pyinstaller pyinstaller --noconfirm --clean --onedir --add-data ../../../vera-doc/src/vera/ingest/tessdata:vera/ingest/tessdata --name vera-sidecar --distpath build/sidecar --workpath build/pyinstaller --specpath build/pyinstaller src/vera_app/sidecar.py",
    "dist": "npm run build && npm run build:sidecar && electron-builder",
    "typecheck": "npm run typecheck:electron && npm run typecheck:renderer",
    "typecheck:electron": "tsc -p tsconfig.electron.json --noEmit",
    "typecheck:renderer": "tsc -p tsconfig.renderer.json",
    "test:unit": "vitest run",
    "start": "electron ."
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "lucide-react": "latest",
    "pdfjs-dist": "^6.0.227",
    "react": "latest",
    "react-dom": "latest",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "concurrently": "latest",
    "electron": "latest",
    "electron-builder": "latest",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "^4.1.10",
    "wait-on": "latest"
  },
  "build": {
    "appId": "dev.vera.desktop",
    "productName": "VERA",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**",
      "dist-electron/**",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "build/sidecar/vera-sidecar",
        "to": "python/sidecar"
      },
      {
        "from": "src/vera_app/modes_builtin",
        "to": "python/vera_app/modes_builtin"
      }
    ],
    "win": {
      "target": "nsis"
    }
  }
}
